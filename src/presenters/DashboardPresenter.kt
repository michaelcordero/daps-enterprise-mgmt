package presenters

import application.dq
import database.queries.DataQuery
import model.User
import security.DAPSSession

class DashboardPresenter (dq: DataQuery): AbstractPresenter(dq) {
//    fun fullName(): String = user().first_name + user().last_name
}
